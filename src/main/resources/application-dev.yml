debug: true

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/linkedface/
    username: root
    password: 957966
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 10
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      max-lifetime: 1900000
      connection-timeout: 30000
      connection-test-query: SELECT 1
      pool-name: LinkedFaceHikariCP



server:
  servlet:
    context-path: /linkedface